# AGENTS.md — KM 项目协作约定

## 任务结束统计（每次任务交付时必须附加）

每次任务（含子任务）结束时，在最终回复末尾附「任务统计」小节，固定包含以下四项：

- **耗时**：任务起止墙钟时间（用环境时间 `date` 统计，给出分钟级即可）。
- **Token 消耗**：CLI 不向模型暴露精确 token 数据，给出粗略估算（按输出量与工具结果量级估计），并注明"精确值以 CLI 界面/账单页为准"。
- **费用估算**：按 token 估算 × 当前模型单价折算；单价不确定时给出区间或注明假设，不得虚构精确金额。
- **失败情况（若有）**：过程中遇到的失败、重试、降级方案（如子代理配额受限、环境缺工具、构建未跑通）；无失败则写"无"。

示例格式：

```
---
任务统计：耗时 ~X 分钟 | token 估算 ~Y（以 CLI 统计为准） | 费用估算 ~Z 元（按 <单价假设> 折算） | 失败情况：<描述 或 无>
```
